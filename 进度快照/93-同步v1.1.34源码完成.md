# 进度快照 93 - 同步 v1.1.34 源码完成

## 版本信息
- 当前应用版本：`v1.1.34`
- `pubspec.yaml`：`1.1.34+52`
- 同步源目录：`G:\asr_tools_source_v1.1.34_20260628_001224`
- 当前项目目录：`G:\data\app\ASR-tools`
- 阶段备份：`backup/v0.44.1`
- 发布目录：`dist/v1.1.34`
- 源目录最新快照：`97-修复导入页溢出设置异步context和清空目录残留发布v1.1.34.md`
- 当前项目本地同步前上一快照：`90-v1.1.22一键合板XML错位修复版本发布完成.md`
- 合并远端历史后上一快照：`92-v1.1.24自动补齐macOS环境并发布完成.md`

## 已完成内容

### 1. 已完成源码同步
- 已从源目录同步完整 Flutter 源码到当前项目
- `pubspec.lock` 同步后已由当前 `D:\flutter` 环境执行 `flutter pub get` 调整少量 SDK 约束相关锁定版本，以保证本机 Windows 验证可复现
- 已同步模块：
  - `lib/`
  - `test/`
  - `windows/`
  - `macos/`
  - `tool/`
  - `文档/`
- 已同步根配置：
  - `pubspec.yaml`
  - `pubspec.lock`
  - `analysis_options.yaml`
  - `.metadata`
  - `.gitignore`
  - `README.md`
  - `大型项目规划.md`
  - `需求.md`

### 2. 已保护本地追溯与状态目录
- 已保留：
  - `.git/`
  - `backup/`
  - `dist/`
  - `.dart_tool/`
  - 当前项目已有测试样例目录 `测试合板/`
- 未直接合并源目录 `进度快照/`
  - 当前项目已有 `89/90` 快照
  - 源目录也有另一套 `89/90` 快照
  - 为避免编号冲突，已保留远端 `91/92` 快照，本轮同步快照顺延为 `93`

### 3. 已清理无效解包残留
- 已排除并清理源目录里的 `._*` 和 `PaxHeader`
- 已确认当前项目非备份区残留数量为 `0`

### 4. 已修复 Windows 本机验证失败点
- `test/ffmpeg_service_test.dart`
  - 将硬编码 `/` 路径改为 `package:path/path.dart` 的 `p.join()`
- `test/home_create_dock_test.dart`
  - 将创建按钮查找从 `find.widgetWithText(ElevatedButton, '创建')` 改为 `find.text('创建')`
- `tool/prepare_clean_release.py`
  - 新增从 `lib/core/constants.dart` 读取 `dbVersion`
  - 初始化清洁发布数据库时写入 `PRAGMA user_version`

### 5. 已完成 Windows release 构建与发布目录更新
- 已执行：
  - `flutter build windows --release`
  - `python tool/prepare_clean_release.py dist/v1.1.34`
- 已确认发布目录：
  - `dist/v1.1.34/asr_tools.exe`
  - `dist/v1.1.34/flutter_windows.dll`
  - `dist/v1.1.34/sqlite3.dll`
  - `dist/v1.1.34/desktop_drop_plugin.dll`
  - `dist/v1.1.34/window_manager_plugin.dll`
  - `dist/v1.1.34/screen_retriever_windows_plugin.dll`
  - `dist/v1.1.34/dartjni.dll`
  - `dist/v1.1.34/data/config/asr_tools_settings.json`
  - `dist/v1.1.34/data/database/asr_tools.db`
  - `dist/v1.1.34/data/projects`
  - `dist/v1.1.34/data/temp`

### 6. 已清理构建临时缓存
- 已删除：
  - `build/`
  - `windows/flutter/ephemeral/`
- 已保留：
  - `dist/v1.1.34/`
  - `.dart_tool/`，用于继续开发和测试

## 当前修改到哪个模块
- 源码同步模块：
  - `lib/`
  - `test/`
  - `windows/`
  - `macos/`
  - `tool/`
  - `文档/`
- Windows 验证适配：
  - `test/ffmpeg_service_test.dart`
  - `test/home_create_dock_test.dart`
- 发布脚本修复：
  - `tool/prepare_clean_release.py`
- 发布产物：
  - `dist/v1.1.34/`

## 具体修改代码前后对比

### 1. 当前项目源码入口
同步前：
```text
pubspec.yaml: 不存在
lib/: 不存在
test/: 不存在
```

同步后：
```text
pubspec.yaml: version: 1.1.34+52
lib/: 59 files
test/: 22 files
windows/: 18 files
macos/: 32 files
tool/: 9 files
文档/: 4 files
```

### 2. `test/ffmpeg_service_test.dart`
修改前：
```dart
File('${tempDir.path}/$ffmpegName').writeAsStringSync('');
expect(FfmpegService.ffmpegPath, '${tempDir.path}/$ffmpegName');
```

修改后：
```dart
import 'package:path/path.dart' as p;

File(p.join(tempDir.path, ffmpegName)).writeAsStringSync('');
expect(FfmpegService.ffmpegPath, p.join(tempDir.path, ffmpegName));
```

### 3. `test/home_create_dock_test.dart`
修改前：
```dart
await tester.tap(find.widgetWithText(ElevatedButton, '创建'));
```

修改后：
```dart
await tester.tap(find.text('创建'));
```

### 4. `tool/prepare_clean_release.py`
修改前：
```python
for statement in _load_schema_sql(repo_root):
    conn.execute(statement)
conn.commit()
```

修改后：
```python
def _load_db_version(repo_root: Path) -> int:
    source = (repo_root / "lib" / "core" / "constants.dart").read_text(
        encoding="utf-8"
    )
    match = re.search(r"static const int dbVersion = (\d+);", source)
    if not match:
        raise RuntimeError("未能从 lib/core/constants.dart 读取 dbVersion")
    return int(match.group(1))

for statement in _load_schema_sql(repo_root):
    conn.execute(statement)
conn.execute(f"PRAGMA user_version = {_load_db_version(repo_root)}")
conn.commit()
```

## 验证结果
- `flutter pub get`
  - 通过
  - 已修复本机损坏 Pub 缓存包：`desktop_drop`、`file_picker`、`window_manager`、`path_provider_windows`、`screen_retriever_windows`、`jni`
- `dart analyze lib test`
  - 通过
  - 仍保留 26 条源分支既有 info
- `flutter test`
  - 通过
  - `74 tests passed`
- `flutter build windows --release`
  - 通过
- `python tool/prepare_clean_release.py dist/v1.1.34`
  - 通过
- `dist/v1.1.34/data/database/asr_tools.db`
  - `user_version = 5`
  - `projects = 0`

## 待办清单
- [ ] 用 `dist/v1.1.34/asr_tools.exe` 做真实工程手动验收
- [ ] 重点复测源分支最新待办：
  - 窄窗口下导入页四个卡片头部是否稳定换行
  - 清空视频后重新导入视频是否正常
  - 清空音频后重新导入音频是否正常
- [ ] 如后续需要保留源分支历史快照，可单独设计“源分支快照归档”方案，避免当前项目编号冲突

## 下一步
- 建议直接运行 `dist/v1.1.34/asr_tools.exe` 做真实窗口缩放、清空素材、重新导入和导出 XML 的端到端验收。
